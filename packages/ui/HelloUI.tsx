import React from 'react';
import { Text } from 'react-native';

export const HelloUI = () => (
  <Text style={{ color: 'teal', fontWeight: 'bold', fontSize: 18 }}>
    Hello from UI package!
  </Text>
);

export default HelloUI;
